#include <stdio.h>

int ispwr(int n) 
{
return n > 0 && (n & (n - 1)) == 0;
}

int main( ) 
{
int n;

printf("Введите число: ");

scanf ("%d", &n);

int result = ispwr (n);

printf("%d\n", result);

return 0;

}
